{
  "name": "bimnp-os",
  "version": "1.0.0",
  "description": "BIMNP Enterprise Intelligence OS",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node dist/server.js",
    "build": "mkdir -p dist && esbuild index.tsx --bundle --format=esm --outfile=dist/index.js --minify --external:react --external:react-dom --external:lucide-react --external:recharts --external:@google/genai && cp index.html manifest.json service-worker.js server.js dist/"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}